<p id="notice"><%= notice %></p>

<!DOCTYPE html>
<html>
<%= render 'chartHead' %>
<body>
	<%= render 'header' %>
<div class="page-wrapper">
  
	<section class="container">
		  # render in rails views
		  <div class="row">
		    <%= render 'calendar' %>
		    <%= react_component 'Main'  %>
		  </div><!-- row -->
      
	</section>
</div>









<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @job_sites.each do |job_site| %>
      <tr>
        <td><%= link_to 'Show', job_site %></td>
        <td><%= link_to 'Edit', edit_job_site_path(job_site) %></td>
        <td><%= link_to 'Destroy', job_site, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
The Rainrate for [<%=@coordinates%>], (Henderson, NV) is <%=@testresults['rainRate']%>
<%= link_to 'New Job site', new_job_site_path %>


</body>
</html>